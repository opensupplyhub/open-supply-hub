name: "DB - Apply anonimized db"

on:
  workflow_dispatch:

jobs:
  save-anonimized-db:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Save Anonimized DB
        run: |
          echo "Apply Anonimized DB"
          echo "DB applied!"