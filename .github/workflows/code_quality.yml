name: Code Quality

on:
  pull_request:
    types:
      - synchronize
      - opened
      - reopened

jobs:
  code_quality_frontend:
    uses: ./code_quality_frontend.yml

  code_quality_django:
    uses: ./code_quality_django.yml

  code_quality_contricleaner:
    uses: ./code_quality_contricleaner.yml

    # run-bash-script-linter:
    #   environment: Quality Environment
    #   runs-on: ubuntu-latest
    #   steps:
    #     - name: Checkout
    #       uses: actions/checkout@v4
    #     - name: Run the shellcheck linter check
    #       run: shellcheck scripts/*
