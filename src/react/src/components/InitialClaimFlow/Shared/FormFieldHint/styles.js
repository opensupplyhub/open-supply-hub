const styles = theme =>
    Object.freeze({
        hint: Object.freeze({
            fontSize: '14px',
            color: theme.palette.text.secondary,
            margin: '-8px 0 8px 0',
        }),
    });

export default styles;
