const styles = theme =>
    Object.freeze({
        title: Object.freeze({
            fontSize: '18px',
            fontWeight: theme.typography.fontWeightSemiBold,
            margin: '0 0 8px 0',
        }),
    });

export default styles;
