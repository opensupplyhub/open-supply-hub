export default theme =>
    Object.freeze({
        container: {
            backgroundColor: 'white',
        },
        title: Object.freeze({
            marginBottom: theme.spacing.unit,
        }),
    });
