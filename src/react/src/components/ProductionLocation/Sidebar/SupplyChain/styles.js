export default theme =>
    Object.freeze({
        container: Object.freeze({
            backgroundColor: 'white',
        }),
        title: Object.freeze({
            marginBottom: theme.spacing.unit,
        }),
    });
