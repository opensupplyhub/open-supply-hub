/** Uppercase tracked kicker label above headlines ("CASE STUDY", "FIND OUT MORE"). */
export interface EyebrowProps {
  /** Text color (any token) */
  color?: string;
  children?: React.ReactNode;
}
